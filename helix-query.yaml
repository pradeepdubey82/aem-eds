version: 1
indices:
  eds-test:
   include:
    - "**"
   exclude:
    - /en/**
   target: /query-index
   properties:
     title:
       select: 
         head > meta[property="og:title"]
       value: |
        attribute(el, 'content')